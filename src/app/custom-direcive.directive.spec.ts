import { CustomDireciveDirective } from './custom-direcive.directive';

describe('CustomDireciveDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomDireciveDirective();
    expect(directive).toBeTruthy();
  });
});
